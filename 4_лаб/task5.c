#include <stdio.h>
#include "func.h"

void sortStackArray(int* arr, int n)
{
	//	int arr[N];
	printf("%d \n", N);
	readArray(arr, N);
	writeArray(arr, N);
	sort(arr, n);
	writeArray(arr, N);

}